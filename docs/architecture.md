# Architecture

OpenCamille — a personal AI agent.

## Overview

**Entry point:** CLI tool (`src/index.ts`)
**Config:** Env vars validated via zod (`src/config.ts`)
**Runtime:** Node.js, TypeScript, ESM

> This document evolves as architecture decisions are recorded in `adr/`.

## Current State

_Architecture decisions in progress. See `adr/` for recorded decisions._
